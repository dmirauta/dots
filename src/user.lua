return {
  -- You can also add new plugins here as well:
  -- Add plugins, the lazy syntax
  -- {
  --    "nvim-telescope/telescope.nvim",
  --     defaults = {
  --         vimgrep_arguments = {
  --           "rg",
  --          }
  --     }
  -- }
}
